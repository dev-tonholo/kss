product:
  type: lib
  platforms:
    - jvm

apply:
  - ../kss.module-template.yaml

settings:
  publishing:
    enabled: true
    group: "dev.tonholo.kss"
    version: "1.0.0"
    name: "kss-bom"

dependencies:
  - ../core: exported
  - ../lexer: exported
  - ../parser: exported
