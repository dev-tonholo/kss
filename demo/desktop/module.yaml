product:
  type: jvm/app

apply:
  - ../../kss.module-template.yaml

settings:
  compose:
    enabled: true

dependencies:
  - ../shared
  - $compose.desktop.currentOs
  - $libs.org.jetbrains.kotlinx.coroutinesSwing
