# demo/desktop — Desktop Demo App

CRITICAL: MUST read and follow `AGENTS.md` in this directory for module-specific
conventions.

Desktop entry point (JVM) for the KSS demo application.

```bash
./amper build -m demo/desktop
```
