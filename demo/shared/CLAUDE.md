# demo/shared — Shared Demo UI

CRITICAL: MUST read and follow `AGENTS.md` in this directory for module-specific
conventions.

Shared Compose Multiplatform UI for the KSS demo app (desktop + web).

```bash
./amper build -m demo/shared
./amper test -m demo/shared
```
